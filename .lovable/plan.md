# TOKEN2049 Singapore Week Page

## Scope
- Create a new `/token2049sg` event page using the existing A36 visual system and official logo.
- Add `TOKEN2049 Singapore` to the existing Programs dropdown on desktop and mobile.
- Remove only the temporary Indore/Team1 block from `/links`.
- Add the new route to sitemap and route-level SEO without changing other pages.

## Build
1. Add the uploaded Run the Bay GIF through the project asset system and use it only on the first event card.
2. Build the page with the supplied hero, three event cards, audience cards, partnership package, open partner slots, and contact actions.
3. Keep event links external, preserve exact supplied dates/locations/copy, and add lightweight reveal, cursor halo, card depth, and reduced-motion behavior.
4. Add Event structured data for the three events using only supplied facts and Luma URLs.
5. Update Programs navigation in both desktop and mobile menus, and mark it active on the new route.
6. Remove the concluded Indore/Team1 feature block while preserving the normal `/links` content.

## Validation
- Confirm all three Luma URLs, partnership email/Telegram links, canonical metadata, and structured data.
- Test 320, 360, 375, 390, 430, and desktop widths for overflow, card readability, and tap targets.
- Verify touch and reduced-motion behavior, runtime rendering, TypeScript, tests, and production build.
